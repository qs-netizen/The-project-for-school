const canvas=document.getElementById("space"),ctx=canvas.getContext("2d");let stars=[];function resize(){canvas.width=innerWidth;canvas.height=innerHeight;stars=Array.from({length:180},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*1.4,a:.2+Math.random()*.7,s:.1+Math.random()*.35}))}resize();addEventListener("resize",resize);function draw(){ctx.clearRect(0,0,canvas.width,canvas.height);for(const p of stars){p.y+=p.s;if(p.y>canvas.height)p.y=0;ctx.globalAlpha=p.a;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle="#dceaff";ctx.fill()}ctx.globalAlpha=1;requestAnimationFrame(draw)}draw();function go(id){document.getElementById(id)?.scrollIntoView({behavior:"smooth"})}document.querySelectorAll("a[href^='#']").forEach(a=>a.addEventListener("click",e=>{const id=a.getAttribute("href").slice(1);if(document.getElementById(id)){e.preventDefault();go(id)}}));
